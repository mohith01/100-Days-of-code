class Solution:
    def numJewelsInStones(self, jewels: str, stones: str) -> int:
        count = 0
        s = list(jewels)
        for i in s:
            count=count+stones.count(i)
            
        return count
